services:
  db:
    image: postgres:17
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: dharma
    ports:
      - "5432:5432"
    volumes:
      - dharma_pg_data:/var/lib/postgresql/data

volumes:
  dharma_pg_data:
