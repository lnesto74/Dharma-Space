/** Brand assets — icon bundled locally; works on any host. */
export const LOGO_MARK_URL = "/dharma-logo-mark.png";

/** @deprecated Use LOGO_MARK_URL — kept for small icon-only slots in platform UI */
export const LOGO_URL = LOGO_MARK_URL;

export const FAVICON_URL = LOGO_MARK_URL;
